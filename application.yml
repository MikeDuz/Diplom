spring.datasource.url: jdbc:mysql://localhost:3306/diplom?serverTimezone=UTC#?allowPublicKeyRetrieval=true&amp;shutdown=true&amp;useSSL=false
spring.datasource.username: root
spring.datasource.password: test4321

spring.jpa.show-sql: true
spring:
  jpa:
    properties:
      hibernate:
        validator:
          apply_to_ddl: false
  flyway:
  username: root
  password: test4321
  schemas: db_migrate
  enabled: true
  validate-on-migrate: true
spring.jpa:
  hibernate:
    connection:
      charset: utf8
      useUnicode: true
    ddl-auto: update
  properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect

blog:
  title: WD pub
  subtitle: weeb disign pub
  telegram: MikeVS1
  email: mikevs63@gmail.com
  copyright: Mikhail Smirniskiy
  copyrightFrom: 2021

